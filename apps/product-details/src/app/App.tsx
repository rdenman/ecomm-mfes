import { FC } from 'react';
import { BrowserRouter } from 'react-router-dom';

const App: FC = () => {
  return (
    <BrowserRouter>
      <div>Product Details</div>
    </BrowserRouter>
  );
};

export default App;
